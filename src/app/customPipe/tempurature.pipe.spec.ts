import { TempuraturePipe } from './tempurature.pipe';

describe('TempuraturePipe', () => {
  it('create an instance', () => {
    const pipe = new TempuraturePipe();
    expect(pipe).toBeTruthy();
  });
});
